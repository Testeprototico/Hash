version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=production

  hashcat:
    image: hashcat/hashcat
    entrypoint: ["hashcat", "--version"]
    volumes:
      - ./data:/data
